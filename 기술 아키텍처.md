# 기술 아키텍처

## 🏗️ 전체 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    프론트엔드 (게임 UI)                        │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────┐ │
│  │   모바일 앱      │  │    웹 클라이언트   │  │   PC 앱      │ │
│  │  (React Native) │  │    (React/Vue)   │  │  (Electron)  │ │
│  └────────┬────────┘  └────────┬────────┘  └──────┬───────┘ │
└───────────┼─────────────────────┼─────────────────┼─────────┘
            │                     │                 │
            └─────────────────────┼─────────────────┘
                                  ▼
┌─────────────────────────────────────────────────────────────┐
│                      API Gateway                             │
│                    (인증, 라우팅, 로깅)                        │
└─────────────────────────────────────────────────────────────┘
                                  │
        ┌─────────────────────────┼─────────────────────────┐
        ▼                         ▼                         ▼
┌───────────────┐       ┌───────────────┐       ┌───────────────┐
│  게임 서버     │       │  에이전트 오케   │       │  정산 서비스   │
│  (상태 관리)   │       │  스트레이션     │       │  (블록체인)    │
└───────────────┘       └───────────────┘       └───────────────┘
        │                         │                         │
        ▼                         ▼                         ▼
┌───────────────┐       ┌───────────────┐       ┌───────────────┐
│   PostgreSQL  │       │  Redis Queue  │       │   Polygon/    │
│   (유저 데이터) │       │  (작업 큐)     │       │   Base L2     │
└───────────────┘       └───────────────┘       └───────────────┘
                                  │
                                  ▼
                    ┌─────────────────────────┐
                    │   Worker Containers     │
                    │  ┌─────┐ ┌─────┐ ┌─────┐│
                    │  │User1│ │User2│ │User3││
                    │  │ PC  │ │ PC  │ │ PC  ││
                    │  └─────┘ └─────┘ └─────┘│
                    │   (Docker + OpenClaw)   │
                    └─────────────────────────┘
```

## 🧩 핵심 컴포넌트

### 1. 프론트엔드 (게임 UI)

**기술 스택 옵션**:
| 옵션 | 장점 | 단점 |
|------|------|------|
| **React Native + Expo** | 크로스플랫폼, 빠른 개발 | 복잡한 애니메이션 제한 |
| **Unity** | 풍부한 애니메이션, 게임 느낌 | 앱 용량 큼, 학습 곡선 |
| **Flutter** | 성능 좋음, 예쁜 UI | 게임 느낌 구현 노력 필요 |

**추천**: React Native + Lottie 애니메이션 (MVP)
→ 이후 Unity로 마이그레이션 고려

**핵심 화면**:
- 사무실 뷰 (메인)
- 직원 관리
- 수익 대시보드
- 상점 (스킨, 업그레이드)
- 설정

### 2. 에이전트 오케스트레이션

**기반**: OpenClaw/Moltbot (오픈소스)

```python
# 직원 1명 = 1 Docker 컨테이너
class EmployeeAgent:
    def __init__(self, user_id, job_type):
        self.user_id = user_id
        self.job_type = job_type  # "마케터", "데이터수집가" 등
        self.moltbot = MoltbotInstance()
        self.virtual_pc = VirtualPC()
    
    async def work(self, task):
        # 실제 작업 수행
        result = await self.moltbot.execute(task)
        
        # 게임 이벤트 발생
        self.emit_event("work_completed", {
            "earnings": result.earnings,
            "animation": "typing"  # 프론트에서 타이핑 애니메이션
        })
        
        return result
```

### 3. 가상 PC (per-user)

**구현**:
- Docker 컨테이너로 격리된 환경
- 브라우저 (Playwright) 포함
- 사용자별 세션/쿠키 분리

**스케일링**:
- Kubernetes로 자동 스케일
- 유휴 시 컨테이너 절전 (비용 절감)

### 4. 정산 서비스 (블록체인)

**체인 선택**: Polygon PoS 또는 Base
- 이더리움 메인넷 ❌ (가스비 비쌈)
- Polygon: 가스비 $0.001 미만
- Base: Coinbase 연동 쉬움

**스마트 컨트랙트**:
```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

contract AgentTycoonRevenue {
    address public platformWallet;
    uint256 public platformFeePercent = 10; // 10%
    
    mapping(address => uint256) public userBalances;
    
    function depositRevenue(address user) external payable {
        uint256 platformFee = (msg.value * platformFeePercent) / 100;
        uint256 userShare = msg.value - platformFee;
        
        userBalances[user] += userShare;
        payable(platformWallet).transfer(platformFee);
        
        emit RevenueDeposited(user, userShare, platformFee);
    }
    
    function withdraw() external {
        uint256 amount = userBalances[msg.sender];
        require(amount > 0, "No balance");
        
        userBalances[msg.sender] = 0;
        payable(msg.sender).transfer(amount);
    }
    
    event RevenueDeposited(address indexed user, uint256 userShare, uint256 platformFee);
}
```

## 🔐 보안 고려사항

1. **사용자 자격증명 격리** — 컨테이너별 분리
2. **API 키 암호화** — Vault/KMS 사용
3. **작업 감사 로그** — 모든 에이전트 활동 기록
4. **Rate Limiting** — 플랫폼 ToS 준수

## 📊 인프라 비용 추정 (MVP)

| 항목 | 월 비용 (예상) |
|------|---------------|
| 서버 (10명 동시) | $50-100 |
| DB (PostgreSQL) | $20 |
| Redis | $15 |
| 블록체인 가스비 | $10 |
| **합계** | **~$100/월** |

→ 사용자 100명 되면 월 $500-1000

---

**다음 단계**: [[직원 유형 (에이전트 모듈)]]
