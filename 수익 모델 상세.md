# 수익 모델 상세

## 💰 수익 흐름 개요

```
[사용자가 벌어들이는 수익]
            │
            ▼
┌─────────────────────────────────┐
│        스마트 컨트랙트           │
│   (자동 정산 + 투명성 보장)       │
└─────────────────────────────────┘
            │
    ┌───────┴───────┐
    ▼               ▼
[사용자 지갑]    [플랫폼 수수료]
   (90%)            (10%)
```

---

## 📊 수익 모델 상세

### 1. 광고 모델 (PPL 피켓 시위)

**컨셉**:
- 직원이 "월급이 부족해요..." 하며 피켓 시위
- 광고 시청 = 직원에게 밥(API 크레딧) 주기
- 강제 광고가 아니라 **자발적 선택**처럼 느끼게

**구현**:
```javascript
// 광고 트리거 조건
const shouldShowAd = () => {
  return (
    user.apiCredits < MINIMUM_THRESHOLD ||
    user.lastAdWatch > 24_HOURS_AGO ||
    user.employee.happiness < 30  // 직원 행복도 낮으면
  );
};

// 광고 후 보상
const onAdComplete = (adType) => {
  if (adType === 'rewarded_video') {
    user.apiCredits += 100;
    employee.happiness += 20;
    showAnimation('employee_cheering');
  }
};
```

**광고 유형**:
| 유형 | 보상 | 길이 |
|------|------|------|
| 배너 | 💵 10 | 항상 표시 |
| 전면 광고 | 💵 30 | 5초 |
| 보상형 비디오 | 💵 100 | 15-30초 |
| 플레이어블 | 💵 200 | 인터랙티브 |

**예상 수익 (플랫폼)**:
- eCPM: $5-15 (지역별 차이)
- DAU 1,000명 × 3회/일 = $15-45/일

---

### 2. 월급 모델 (서버비/수수료)

**컨셉**:
- "직원 월급"으로 서버비 부과
- 사용자가 벌어들인 수익에서 자동 차감
- **손해 보는 느낌 ↓** (이미 번 돈에서 나가니까)

**요금 체계**:
| 직원 등급 | 월급 (API 비용) | 수수료 |
|----------|----------------|--------|
| 인턴 | $5/월 | 5% |
| 사원 | $15/월 | 8% |
| 대리 | $30/월 | 10% |
| 과장+ | $50/월 | 12% |

**정산 로직**:
```python
def calculate_payout(user, gross_revenue):
    # 1. 월급 (고정비) 차감
    salary = sum(e.monthly_salary for e in user.employees)
    
    # 2. 수수료 (변동비) 차감
    commission_rate = get_commission_rate(user.tier)
    commission = gross_revenue * commission_rate
    
    # 3. 순수익
    net_revenue = gross_revenue - salary - commission
    
    # 4. 적자 시 처리
    if net_revenue < 0:
        trigger_forced_ads(user)  # 광고 강제 (유쾌하게)
        return 0
    
    return net_revenue
```

**적자 시 UX**:
- 직원들이 "회사가 어려워요..." 애니메이션
- "광고 보면 회사 살릴 수 있어요!" 버튼
- 강제지만 죄책감 유발 → 자발적 시청 느낌

---

### 3. 스마트 컨트랙트 (자동 정산)

**목적**:
1. 투명한 수익 분배
2. 플랫폼 신뢰도 ↑
3. 탈중앙화 이미지

**체인 선택**:
| 체인 | 가스비 | 장점 |
|------|--------|------|
| **Polygon PoS** | ~$0.001 | 저렴, 안정적 |
| **Base** | ~$0.01 | Coinbase 연동 |
| **Arbitrum** | ~$0.05 | 이더리움 호환성 |

**추천**: Polygon PoS (MVP) → Base (확장 시)

**스마트 컨트랙트 기능**:
```solidity
contract AgentTycoon {
    // 수익 분배
    function distributeRevenue(
        address user,
        uint256 amount
    ) external onlyOracle {
        uint256 platformFee = (amount * platformFeePercent) / 100;
        uint256 userShare = amount - platformFee;
        
        // USDC로 정산 (변동성 ↓)
        usdc.transfer(user, userShare);
        usdc.transfer(platformWallet, platformFee);
        
        emit RevenueDistributed(user, userShare, platformFee);
    }
    
    // 누적 수익 조회 (투명성)
    function getTotalEarnings(address user) view returns (uint256);
    
    // 출금
    function withdraw() external;
}
```

---

### 4. 스킨/아이템 판매

**인앱 구매**:
| 아이템 | 가격 | 설명 |
|--------|------|------|
| 캐릭터 스킨 | $1-5 | 외형 변경 |
| 사무실 테마 | $2-10 | 배경 변경 |
| 부스트 아이템 | $0.5-2 | 일시적 효율 ↑ |
| 프리미엄 직원 | $10-30 | 고급 에이전트 해금 |

**💎 젬 시스템**:
- 광고 시청: 💎 10
- 인앱 구매: 💎 100 = $1
- 스킨 구매: 💎 50-500

---

## 📈 수익 예측 (MVP)

**가정**:
- MAU: 1,000명
- 유료 전환율: 5%
- ARPU: $10/월

| 수익원 | 월 수익 |
|--------|---------|
| 광고 | $500 |
| 월급/수수료 | $300 |
| 인앱 구매 | $500 |
| **합계** | **$1,300/월** |

**비용**:
| 항목 | 월 비용 |
|------|---------|
| 서버 | $200 |
| API (OpenAI 등) | $400 |
| 기타 | $100 |
| **합계** | **$700/월** |

**순이익**: ~$600/월 (MVP 단계)

---

## ⚠️ 리스크 & 대응

| 리스크 | 대응 |
|--------|------|
| API 비용 급증 | 로컬 모델 도입 (Ollama) |
| 플랫폼 ToS 위반 | 허용된 자동화만 수행 |
| 환불 요청 | 명확한 수익 보장 없음 고지 |
| 사기 의심 | 스마트컨트랙트로 투명성 |

---

**다음 단계**: [[UI UX 설계]]
